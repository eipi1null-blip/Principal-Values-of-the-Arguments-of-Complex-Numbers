# Complex Argument Applet

Upload the contents of this folder to the root of the GitHub repository used for GitHub Pages.

The applet is a standalone `index.html` file. It lets users drag a purple point in the complex plane and compare `arctan(y/x)` with the correct argument `arg z` across all quadrants.
